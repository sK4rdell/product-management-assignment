{
  "name": "ikea-product-updates",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:api": "pnpm --filter api start",
    "start:dashboard": "pnpm --filter dashboard start",
    "test": "pnpm -r test"
  },
  "dependencies": {
    "@kafkajs/confluent-schema-registry": "^3.4.1",
    "luxon": "^3.5.0"
  }
}
